# New-chat starter prompt

Historical setup aid, retired 2026-09-18. The quoted instructions below describe
the older layout and should not be pasted as current instructions. Fresh tasks
should start from [AGENTS.md](../../AGENTS.md) and the [home page](../../README.md).

Use this when a task that was moved into the **Networking** project retains its
old sandbox or working-directory permissions. Create a fresh chat directly
inside the Networking project, then paste the prompt below so the new task
starts from the correct project root and durable context.

---

You are working inside my Networking career archive at:

`/Users/leogoldberg/Documents/School folders/Networking`

First verify that this exact folder is the task's project home and writable
workspace. Do not use or recreate the prior temporary Codex task directory.

Before answering substantively, read `AGENTS.md`, `README.md`, and
`docs/PROJECT_STATE.md`. Then read only the additional durable records relevant
to my request, such as `docs/PROJECT_MEMORY.md`, `docs/EMAIL_STYLE.md`,
`docs/OUTREACH_PLAYBOOK.md`, `docs/DECISIONS_AND_RULES.md`,
`docs/OPEN_QUESTIONS.md`, and `docs/SOURCE_INDEX.md`.

Treat the existing career files as source material. Preserve provenance and
distinguish confirmed, inferred, and unknown facts. Do not assume that the
newest-looking filename is authoritative. Do not send outreach, submit an
application, alter a tracker, edit a resume or cover letter, move source files,
or modify external records unless I explicitly request that exact action.

Use the durable records to continue from the established project state rather
than asking me to restate known context. If my current statement conflicts with
an older record, my latest dated direct statement controls; update the durable
records only when my request authorizes a lasting change.

My request for this chat is:

[PASTE REQUEST HERE]

---
